# Alternative deployment configuration for ApprenticeTracker
# Uses production-server.js which properly loads compiled JavaScript

[deployment]
deploymentTarget = "autoscale"
build = ["sh", "-c", "npm run build"]
run = ["sh", "-c", "NODE_ENV=production node production-server.js"]
