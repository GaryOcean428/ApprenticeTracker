Promotion failed
Your deployment attempt had the following errors:
Health check to the root path (/) is failing
Fair Work API integration is not properly configured
No health check endpoint is implemented in the server
Assistant suggestions
import express from 'express';

// Add other imports

const app = express();

// Add your existing middleware

// Add a health check route at the root path
app.get('/', (req, res) => {
  res.status(200).send('OK');
});

// Add your other routes
// Ensure your FAIRWORK_API_KEY and FAIRWORK_API_URL are set in your secrets
// For example, in your deployment configuration page:
// FAIRWORK_API_KEY=your_api_key_here
// FAIRWORK_API_URL=https://api.fairwork.gov.au/v1

// Then verify the values are being loaded correctly in your code:
console.log('FAIRWORK_API_URL is set:', !!process.env.FAIRWORK_API_URL);
// Don't log the actual API key, just check if it exists
console.log('FAIRWORK_API_KEY is set:', !!process.env.FAIRWORK_API_KEY);
// Start the sync in the background
syncScheduler.triggerSync({
  forceUpdate: !!forceUpdate,
  targetAwardCode: awardCode,
})
  .then(() => {
    logger.info('Manual Fair Work data sync completed successfully');
  })
  .catch(error => {
    logger.error('Error in manual Fair Work data sync', { error });
  });
// Start the sync in the background
try {
  syncScheduler.triggerSync({
    forceUpdate: !!forceUpdate,
    targetAwardCode: awardCode,
  })
    .then(() => {
      logger.info('Manual Fair Work data sync completed successfully');
    })
    .catch(error => {
      logger.error('Error in manual Fair Work data sync', { error: error.message });
    });
} catch (err) {
  logger.error('Failed to start sync scheduler', { error: err.message });
  // Continue execution, don't let this crash the application
}
// Initialize Fair Work API client
try {
  const fairWorkApiClient = new FairWorkApiClient({
    apiKey: process.env.FAIRWORK_API_KEY,
    apiUrl: process.env.FAIRWORK_API_URL,
  });

  // Optional: Test the connection at startup but don't block application startup
  fairWorkApiClient.testConnection()
    .then(() => logger.info('Fair Work API connection successful'))
    .catch(err => logger.warn('Fair Work API connection failed, will retry on requests', { error: err.message }));
} catch (err) {
  logger.error('Failed to initialize Fair Work API client', { error: err.message });
  // Continue application startup, services will handle missing client gracefully
}
Production
Status
Braden deployed 16 days ago
Domain
https://apprentice-tracker-bradenlang77.replit.app
Type
Autoscale
(4 vCPU / 8 GiB RAM / 3 Max)
See all usage
dd097549
BL

bradenlang77
Braden
deployed 17 days ago
3f0052fa
BL

bradenlang77
Braden
deployed 22 days ago
12472914
BL

bradenlang77
Braden
deployed 22 days ago
934347aa
BL

bradenlang77
Braden
deployed 23 days ago